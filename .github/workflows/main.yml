name: CI de Qualidade e Testes em Python

on:
  push:
    branches: 
      - main
      - 'feature/**' # Aciona em 'feature/containerization', 'feature/outra-coisa', etc.
  pull_request:
    # Por padrão, PRs para QUALQUER branch rodam o Action.
    branches: [ "main" ]

jobs:
  qualidade_de_codigo:
    # 1. Este Job se concentra na qualidade e estilo do código (Linting)
    runs-on: ubuntu-latest
    
    steps:
      - name: ⬇️ Checkout do Codigo
        uses: actions/checkout@v4

      - name: 🐍 Configurar Ambiente Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: 🛠️ Instalar Dependencias (Flask e Flake8)
        # Instalamos Flask, pytest e flake8
        run: pip install -r requirements.txt

      - name: 🧹 Executar Linting (Flake8)
        # O Flake8 verifica se o codigo segue as diretrizes de estilo (PEP 8).
        # Se houver erros, este passo falha e interrompe o pipeline.
        run: |
          echo "Verificando qualidade do código com Flake8..."
          # ignore: E501 (linha muito longa, comum em exemplos)
          flake8 . --count --exit-zero --exclude=tests/* --max-complexity=10 --max-line-length=120 --statistics

  testes_funcionais:
    # 2. Este Job se concentra na funcionalidade e logica do codigo
    runs-on: ubuntu-latest
    # Define que este Job SÓ PODE rodar se o Job de qualidade de codigo for bem-sucedido
    needs: qualidade_de_codigo 
    
    steps:
      - name: ⬇️ Checkout do Codigo
        uses: actions/checkout@v4
      
      - name: 🐍 Configurar Ambiente Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: 🛠️ Instalar Dependencias (Flask e Pytest)
        run: pip install -r requirements.txt

      - name: 🧪 Executar Testes Unitarios (Pytest)
          # CONFIGURAÇÃO DE AMBIENTE:
          # Exporta o diretório atual do GitHub Runner (que é a raiz do projeto)
          # para a variável PYTHONPATH. Isso permite que o Python encontre 'app.py'.
        run: |
          echo "Executando testes unitários..."
          export PYTHONPATH=$PWD:$PYTHONPATH
          pytest

  build_and_push_docker: # Renomeamos o Job
    runs-on: ubuntu-latest
    needs: testes_funcionais 
    
    steps:
      - name: ⬇️ Checkout do Codigo
        uses: actions/checkout@v4

      - name: 🐳 Login no Docker Hub
        # Esta acao oficial usa as Secrets do GitHub para fazer login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          
      - name: 🐋 Setar Variaveis de Imagem
        # Cria uma variavel com a TAG e nome da imagem: ex: SEU_USUARIO/projeto-ci-python:latest
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKER_USERNAME }}/projeto-ci-python
          tags: |
            type=raw,value=latest
            type=sha,format=short
            
      - name: 🔨 Build e Push da Imagem Docker
        uses: docker/build-push-action@v5
        with:
          context: . # Usa o diretorio atual para o Dockerfile
          push: true # Faz o push da imagem apos o build
          tags: ${{ steps.meta.outputs.tags }} # Usa as tags definidas acima
          labels: ${{ steps.meta.outputs.labels }}
